# Local PostgreSQL. Replace the password and URL-encode it in DATABASE_URL.
CNPAF_DB_USER=cnpaf
CNPAF_DB_PASSWORD=replace-with-a-unique-local-password
CNPAF_DB_NAME=cnpaf
DATABASE_URL=postgres://cnpaf:replace-with-a-unique-local-password@localhost:5432/cnpaf
APP_PORT=3000

# Optional demo identities. Use a unique value with at least 12 characters.
SEED_PASSWORD=

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1/chat/completions

STORAGE_BACKEND=local
UPLOAD_DIR=./uploads
